logging:
  level:
    root: info
    com.czetsuyatech: debug

server:
  port: 8081
  servlet:
    context-path: /api

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/czetsuyatech
